import Button from './base';

# Button

## Buttons are everywhere

This docs file exists in storybook-docs spike, under ui/components/Button/docs.mdx

This is some paragraph content

This is some code in a &lt;code&gt; tag: <code>import React from "react"</code>

But then things get weird if you put a classname on it? <code className="some-arbitrary-value-that-has-no-meaning">why is the mere presence of 'className' a trigger for this view?</code>

- This
- is a
- bulleted list

1. This
1. is an
1. ordered list

_this is italic text_

**this is some bold text, too**

### This is the component, rendered live!

<Button>Button</Button>
